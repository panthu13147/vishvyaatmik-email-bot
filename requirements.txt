# Core
python-dotenv>=1.0.0
requests>=2.31.0

# AI brains
groq>=0.11.0

# Optional fallback brain (only used if GEMINI_API_KEY is set in .env).
# This is the new unified Gemini SDK -- it replaced the deprecated
# google-generativeai package.
google-genai>=0.9.0

# HTML email parsing (fallback when an email has no text/plain part)
beautifulsoup4>=4.12.0
